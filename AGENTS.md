# AI 金融白皮書遊戲化學習挑戰平台 - AGENTS.md

## 1. 專案概述
本專案為「AI 金融白皮書遊戲化學習挑戰平台」，旨在透過高度互動的遊戲化設計，將 SDGs 與 ESG 等相關金融知識轉化為具備深度的學習體驗。
平台核心原則為**鼓勵穩定學習、討論與好奇心**，嚴禁使用賭博機制、羞辱式排名或成癮型黑暗設計。

### 1.1 產品風格 (UI/UX)
* **平台名稱**: 用永續知識，做永續之事。
* **視覺主題**: ESG / SDGs 永續、自然森林與大地。
* **核心色調**: 森林大地綠色系 —— 深松綠、森林綠、萊姆葉綠為主，暖米白、淺木、木質棕為大地點綴，藍色僅保留作 SDG 水資源點綴。
* **配色實作**: 統一定義於 `frontend/src/index.css` 的 `:root` design tokens（`--green-1..5`、`--earth-1..3`、`--gold*` 已對應綠色等）。調整顏色請優先改 token，不要在元件硬寫色碼。
* **介面元素**: 乾淨的 Dashboard 卡片、徽章、進度環、專業學習遊戲氛圍，以及 10-20 秒的高質感升級蛻變動畫。

---

## 2. 系統架構現況
* **前端框架**: React 19 (Vite) + Vanilla CSS (保留現有樣式，未使用 Tailwind)。
* **後端框架**: Node.js + Express + Socket.io。
* **資料庫**: 
  * 前端: Firebase Firestore (目前用於 Users)。
  * 後端: 本地 `banks.json` (題庫持久化)。
* **驗證方式**: Firebase Auth (Google 登入、信箱登入)。
* **部署設定**: `render.yaml` (後端可能部署於 Render), `firebase.json` (前端部署於 Firebase Hosting)。

---

## 3. 目標功能清單與優化項目

### 3.1 題庫式學習世界 (Worlds & Stages)
- [ ] **20 個大世界架構**: 擴展現有題庫系統，支援 20 個主題世界。
- [ ] **題目容量**: 每個世界包含 200 題。
- [ ] **階段劃分**: 每個世界分成 10 個階段，每階段 20 題。
- [ ] **進度解鎖**: 完善階段完成度與解鎖機制。

### 3.2 用戶與身份系統 (User Identity)
- [ ] **註冊與分身選擇**: 擴展現有 AuthModal，加入自訂/選擇虛擬分身 (Avatar) 與身份的功能。
- [ ] **遊玩頻率設定**: 讓用戶可選擇「每週遊玩頻率」，系統據此推播適當的進度目標。
- [ ] **個人成就系統**: 新增個人進度、稱號、徽章、令牌、服裝系統的資料結構與 UI。
- [ ] **升級蛻變動畫**: 當用戶升級或達成重大成就時，播放 10-20 秒的特效動畫。

### 3.3 學習與防護機制 (Learning & Protection)
- [ ] **錯題複習機制**: 自動記錄答錯的題目，建立專屬錯題本。
- [ ] **每週進度追蹤**: Dashboard 支援圖表化顯示每週學習進度。
- [ ] **防沉迷機制**: 避免過度遊玩，實作溫和的休息提醒與單日時間/關卡上限。
- [ ] **季度挑戰與活動結算**: 定期舉辦「季度挑戰輪次」，活動結束後實作結算與獎項發放系統。

### 3.4 遊戲與競賽優化 (Game & Competition)
- [ ] **排行榜優化**: 排行榜僅顯示前 5 名或前 10 名，其餘玩家以百分比或個人名次呈現，避免羞辱式排名。
- [ ] **狀態與提示優化**: 確保遊戲與 Dashboard 皆加入載入中 (Loading)、空狀態 (Empty)、錯誤狀態 (Error) 的 UI 處理。

### 3.5 管理員後台與安全性 (Admin Security)
- [x] 主持人/管理員後台 (Teacher Dashboard)。
- [ ] **高安全性登入**: 移除前端 App.jsx 中硬編碼的密碼 (`teacher123`)。
  * **實作方向**: 將管理員權限驗證移至伺服器端 (Node.js API 或 Firebase Functions/Firestore Rules)。
  * **環境變數**: 敏感設定 (如管理員核准密鑰) 需使用 `.env` 環境變數。
  * **Bundle 防護**: 確保管理員密碼或秘密不會被打包至前端程式碼。

---

## 4. 開發規範 (Development Rules)

1. **增量開發**: 除非絕對必要，不要重寫整個專案。優先進行小而可審查的修改。
2. **語言選擇**: 維持目前的 JavaScript (ES Modules) 生態。
3. **響應式設計 (RWD)**: UI 必須同時支援手機 (Mobile) 與桌機 (Desktop)。
4. **狀態處理**: 所有 API 請求與資料載入都必須加入載入 (Loading)、空狀態 (Empty) 與錯誤狀態 (Error)。
5. **資安規範**: 
   * 不要硬編碼任何密鑰。
   * 敏感設定使用環境變數。
   * 管理員權限必須在伺服器端驗證。
   * 避免將敏感資料暴露至前端 bundle。
6. **測試與品質**:
   * 每個主要功能都要加入測試或手動測試步驟。
   * 提交前執行 lint (`npm run lint`)。
7. **修改說明**: 每次任務完成後，需清楚說明修改了哪些檔案。

---

## 5. 執行規劃藍圖 (Execution Phases)

* **Phase 1: 資安漏洞修補與基礎架構擴充**
  * 修復前端硬編碼密碼問題，建立後端/Firebase 的安全 Role 驗證。
  * 設置環境變數檔案架構。
* **Phase 2: 題庫世界資料建模**
  * 實作 20 個世界、階段與題庫的資料庫 Schema (Firestore)。
  * 更新教師後台以上傳/管理世界階層資料。
* **Phase 3: 用戶分身與成就系統**
  * 實作註冊流程中的 Avatar 選擇、每週目標設定。
  * 實作個人進度追蹤與稱號、徽章、令牌介面。
* **Phase 4: 學習機制與防沉迷**
  * 實作錯題本紀錄。
  * 建立防沉迷邏輯與升級蛻變動畫 (CSS/Canvas 動畫)。
* **Phase 5: 競賽排行榜與季度挑戰**
  * 優化排行榜顯示邏輯。
  * 實作季度挑戰結算系統與獎勵發放機制。
